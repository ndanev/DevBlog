<% include partials/header %>

<h1>Index page</h1>

<% blogs.forEach(function(blog) { %>
    <div>
        <h1><%= blog.title %></h1>
        <img src="<%= blog.image %>">
        <p> <%= blog.body %></p>
        <span><%= blog.created %></span>
    </div>
<% }) %>

<% include partials/footer %>
