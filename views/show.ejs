<% include partials/header %>

    <div class="container">
        <div class="card text-center">
            <div class="card-header">
                <h3><%= blog.title %></h3>
            </div>
            <div class="card-body">
                <img src="<%= blog.image %>" class="img-thumbnail img-fluid mb-3">
                <p><%= blog.body %></p>
                <a class="ui green basic button" href="/blogs/<%= blog._id %>/edit">Edit</a>
                <form id="delete" action="/blogs/<%= blog._id %>?_method=DELETE" method="POST">
                    <button class="ui red basic button">DELETE</button>
                </form>
            </div>
            <div class="card-footer">
                <span class="badge badge-light"><%= blog.created.toDateString() %></span>
            </div>
        </div>
    </div>



        <!-- <h1><%= blog.title %></h1>
<img class="img-thumbnail img-fluid" src="<%= blog.image %>">
<p><%- blog.body %></p>
<span><%= blog.created.toDateString() %></span>
<div class="text-center">
    <a class="ui green basic button" href="/blogs/<%= blog._id %>/edit">Edit</a>
</div>
<form action="/blogs/<%= blog._id %>?_method=DELETE" method="POST">
    <button class="ui red basic button">DELETE</button>
</form> -->